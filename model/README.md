# Model Artifacts

Put milestone model checkpoints here when they should be versioned with the
repository. Routine training runs, logs, and scratch checkpoints should stay in
`outputs/`, which is ignored by Git.
